#!/usr/bin/env nextflow
nextflow.enable.dsl=2

// Visualization / report generation workflow — stub
// Planned: Quarto-based HTML report from merged profile tables
workflow VIS {
    main:
    error "Visualization workflow is not yet implemented."
}
