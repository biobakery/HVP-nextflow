#!/usr/bin/env nextflow
nextflow.enable.dsl=2

// Whole Metatranscriptome (MTX) workflow — stub
// Planned: QC → MetaPhlAn → HUMAnN on metatranscriptomic reads
workflow MTX {
    main:
    error "MTX workflow is not yet implemented."
}
